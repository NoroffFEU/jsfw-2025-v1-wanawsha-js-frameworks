export default function Loading() {
    return (
        <main style={{ height: "80vh", display: "flex", flexDirection: "column", justifyContent: "center", alignItems: "center" }}>
            <h1>Online Shop</h1>
            <p>Loading Products...</p>
        </main>
    )
}